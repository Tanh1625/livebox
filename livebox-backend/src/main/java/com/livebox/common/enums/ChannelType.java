package com.livebox.common.enums;

/**
 * ChannelType — discriminates between a Text Channel and a Voice Channel
 * inside a Server.
 *
 * <p>Glossary ref: G02 (Channel), G03 (Text Channel), G04 (Voice Channel).
 */
public enum ChannelType {
    TEXT,
    VOICE
}
